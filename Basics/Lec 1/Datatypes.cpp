#include <bits/stdc++.h>
#include <iostream>
#include <math.h>
#include <string>

using namespace std;

int main()
{
    // // int
    // int x = 10;
    // // long
    // long long y = 10000000000;

    // cout << x << endl;
    // cout << y << endl;

    // string s1, s2;
    // cin >> s1 >> s2;
    // cout << s1 << " " << s2 << endl;

    // Getline can take long sentences string only take a word string
    string str;
    getline(cin, str);
    cout << str << endl;

    return 0;
}
